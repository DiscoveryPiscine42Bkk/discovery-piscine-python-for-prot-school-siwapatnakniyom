# Original array
original_array = [2, 8, 9, 48, 8, 22, -12, 2]

# Filter values greater than 5
filtered_array = [num for num in original_array if num > 5]

# Process (for example, print or modify the values)
print("Filtered values greater than 5:", filtered_array)
